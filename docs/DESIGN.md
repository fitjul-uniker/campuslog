# CampusLog Design Guide

## 1. 디자인 목표

CampusLog 1차 MVP의 디자인 목표는 대학생 사용자가 자신의 활동 경험을 빠르게 찾고, 새 경험을 기록하고, AI 분석과 추천 기능으로 자연스럽게 이어질 수 있는 대시보드형 UI를 만드는 것입니다.

1차 MVP의 첫 화면은 별도 허브형 메인 화면이나 서비스 소개 랜딩이 아니라, 저장된 활동 경험 목록이 바로 보이는 **메인/경험 목록 대시보드**입니다.

핵심 목표는 다음과 같습니다.

- 사용자가 서비스에 들어오면 바로 "내가 기록한 경험"을 볼 수 있어야 합니다.
- 새 경험 기록과 기존 경험 확인이 첫 화면에서 가장 빠르게 가능해야 합니다.
- AI 경험 추천 및 활용 기능은 숨겨진 보조 기능이 아니라, 메인 대시보드에서 자연스럽게 진입 가능한 주요 기능이어야 합니다.
- 1차 MVP에서는 화려한 시각 효과보다 명확한 정보 구조, 안정적인 상태 표현, 예측 가능한 이동 흐름을 우선합니다.
- Codex가 이후 실제 UI를 구현할 때 shadcn/ui 중심으로 빠르게 재현할 수 있는 구조를 기준으로 합니다.

## 2. 디자인 원칙

### 2.1 정보 우선순위

메인/경험 목록 대시보드의 정보 우선순위는 다음 순서로 둡니다.

1. 서비스 이름과 짧은 설명
2. 새 경험 기록하기 버튼
3. 저장된 활동 경험 카드 목록
4. AI 경험 추천 및 활용 진입 버튼
5. 정렬 / 필터
6. 분석 상태 배지와 AI 생성 역량 태그 / 키워드

활동 경험 카드에서는 사용자가 빠르게 스캔할 수 있도록 제목, 기간, 역할 또는 한 줄 설명, 분석 상태, 핵심 태그를 우선 노출합니다. 긴 내용과 성과 상세는 활동 경험 상세 화면에서 확인합니다.

### 2.2 화면 성격

- 대시보드: 저장된 경험을 탐색하고 주요 작업으로 진입하는 화면입니다.
- 작성 / 수정: 경험 원본 데이터를 입력하고 저장하는 폼 화면입니다.
- 상세: 저장된 경험의 원본 내용 확인, 수정, 삭제, AI 분석 요청을 실행하는 화면입니다.
- AI 분석 결과: 특정 경험에 연결된 분석 결과를 확인하는 화면입니다.
- AI 경험 추천 및 활용: 저장된 경험 전체와 분석 결과를 바탕으로 활용 목적에 맞는 경험 1개를 추천받는 화면입니다.

### 2.3 구현 원칙

- shadcn/ui로 구현 가능한 컴포넌트를 우선 사용합니다.
- 카드, 버튼, 입력, 배지, 탭, 셀렉트, 스켈레톤, 토스트, 다이얼로그는 shadcn/ui 기본 패턴을 따릅니다.
- 아이콘은 가능한 경우 Lucide Icons를 사용합니다.
- Magic UI, Aceternity UI, Motion 효과는 1차 MVP 필수 요소가 아니며 future enhancement로만 검토합니다.
- 카드 안에 또 다른 카드가 들어가는 중첩 카드 구조는 피합니다.
- 큰 랜딩 히어로, 마케팅 카피 중심 섹션, 프로필 메뉴, 계정 메뉴는 1차 MVP에 만들지 않습니다.
- 1차 MVP는 localStorage 기반이므로 로그인 사용자 정보, 조직, 팀, 권한 상태를 전제로 한 UI를 만들지 않습니다.

## 3. 참고 레퍼런스

첨부된 Slido dashboard 이미지는 다음 구조만 참고합니다.

- 좌측 사이드바 또는 주요 내비게이션
- 상단 검색 / 액션 영역
- 중앙의 리스트형 카드 목록
- 우측 상단의 Primary CTA 버튼
- 넓은 여백과 정돈된 대시보드형 화면 구성

CampusLog에서는 이를 아래처럼 해석합니다.

| Slido 레퍼런스 구조 | CampusLog 해석 |
|---|---|
| My slidos | 나의 경험 / 경험 목록 |
| Create slido | 새 경험 기록하기 |
| 이벤트 리스트 | 활동 경험 카드 목록 |
| Active and upcoming | 나의 활동 경험 |
| 좌측 메뉴 | 나의 경험, AI 추천 및 활용 정도의 최소 내비게이션 |
| 상단 검색 영역 | 대시보드 상단 액션 영역. 1차 MVP에서는 정렬 / 필터 중심으로 사용 |
| 프로필 / 팀 / 업그레이드 / 외부 연동 | 1차 MVP 제외, future scope |

Slido의 브랜드, 색상, 문구, 메뉴 이름, 조직 / 팀 / 업그레이드 / 연동 구조는 복제하지 않습니다.

## 4. 전체 레이아웃 구조

### 4.1 데스크톱 기본 구조

데스크톱에서는 아래와 같은 앱 셸 구조를 기준으로 합니다.

```text
┌────────────────────────────────────────────────────────────┐
│ 좌측 사이드바 │ 상단 액션 영역                              │
│               ├────────────────────────────────────────────┤
│               │ 메인 콘텐츠                                │
│               │ - 화면 제목 / 짧은 설명                    │
│               │ - 정렬 / 필터 / 주요 CTA                   │
│               │ - 리스트형 경험 카드                       │
│               │ - 빈 상태 / 오류 상태                      │
└────────────────────────────────────────────────────────────┘
```

### 4.2 좌측 사이드바

좌측 사이드바는 1차 MVP에서 최소 메뉴만 제공합니다.

- CampusLog 로고 또는 텍스트 브랜드
- 나의 경험
- AI 추천 및 활용

포함하지 않을 항목:

- 로그인 / 회원가입
- 프로필
- 마이페이지
- 팀 / 조직 관리
- 업그레이드 / 결제
- 외부 서비스 연동
- 관리자 메뉴

권장 Lucide Icons:

- CampusLog 또는 나의 경험: `BookOpen`, `NotebookText`, `Layers`
- AI 추천 및 활용: `Sparkles`
- 새 경험 기록하기: `Plus`
- 대시보드로 돌아가기: `ArrowLeft`

### 4.3 상단 액션 영역

상단 액션 영역은 현재 화면의 주요 작업을 빠르게 실행하는 영역입니다.

메인/경험 목록 대시보드에서는 다음을 둡니다.

- 화면 제목: 나의 활동 경험
- 짧은 설명: 기록한 경험을 다시 보고 AI로 분석하거나 활용해보세요.
- 정렬 옵션
- 필터 옵션
- AI 경험 추천 및 활용 버튼
- 새 경험 기록하기 Primary CTA

검색 입력은 1차 MVP 필수 범위가 아닙니다. 이후 구현 여유가 있을 때 localStorage에 저장된 경험 제목, 역할, 키워드 기준의 클라이언트 검색으로 확장할 수 있습니다.

### 4.4 중앙 리스트 카드

경험 목록은 리스트형 카드를 기본으로 합니다.

- 전체 너비를 과하게 쪼개지 않고, 한 행에 하나의 경험을 보여줍니다.
- 카드 높이는 내용량에 따라 크게 흔들리지 않게 기본 정보를 중심으로 고정감 있게 구성합니다.
- 카드 클릭 시 활동 경험 상세 화면으로 이동합니다.
- 카드 내부의 수정 / 삭제 같은 세부 액션은 1차 MVP에서는 상세 화면에서 처리하는 것을 우선합니다.

### 4.5 우측 상단 CTA

데스크톱에서는 `새 경험 기록하기`를 우측 상단 Primary CTA로 둡니다.

`AI 경험 추천 및 활용`은 같은 상단 액션 영역에 Secondary CTA 또는 강조된 보조 버튼으로 둡니다. AI 기능이 숨겨지지 않도록 사이드바 메뉴와 상단 액션 중 하나 이상에서 접근 가능해야 합니다.

## 5. 화면별 디자인 기준

### 5.1 0. 메인/경험 목록 대시보드

### 목적

사용자가 CampusLog에 들어왔을 때 저장된 활동 경험 목록을 바로 보고, 새 경험 기록, 기존 경험 상세 확인, AI 경험 추천 및 활용 화면으로 이동할 수 있게 합니다.

### 레이아웃

- 데스크톱: 좌측 사이드바 + 상단 액션 영역 + 중앙 리스트 카드
- 태블릿: 사이드바 폭 축소 또는 아이콘 중심 내비게이션
- 모바일: 상단 앱 바 + 접히는 메뉴 또는 하단 주요 액션

### 주요 구성

- 서비스 이름: CampusLog
- 짧은 설명
- 나의 활동 경험 섹션 제목
- 저장된 활동 경험 카드 목록
- 새 경험 기록하기 버튼
- AI 경험 추천 및 활용 버튼
- 정렬 옵션
- 필터 옵션
- 분석 상태 배지
- AI 생성 역량 태그 / 키워드
- 빈 상태 CTA: 첫 경험 기록하기

### 경험 카드 정보 구조

경험 카드에는 다음 순서로 정보를 배치합니다.

1. 제목
2. 기간
3. 역할 또는 한 줄 설명
4. 분석 상태 배지
5. AI 생성 역량 태그 또는 키워드
6. 관련 링크 존재 여부

카드 전체는 클릭 가능한 영역으로 만들되, 버튼처럼 과도하게 보이지 않게 리스트 아이템 패턴을 사용합니다. 키보드 접근 시 focus ring이 보여야 합니다.

### 정렬 / 필터

정렬 옵션:

- 최신순
- 오래된순
- 활동 기간순

필터 옵션:

- 전체
- 미분석
- 분석 완료
- 재분석 필요
- 역량 태그 / 키워드 기준 필터

권장 shadcn/ui:

- `Select`: 정렬 옵션
- `DropdownMenu`: 필터 옵션이 여러 개일 때
- `Badge`: 분석 상태와 태그
- `Button`: 새 경험 기록하기, AI 경험 추천 및 활용
- `Card` 또는 리스트 스타일 `div`: 경험 카드
- `Skeleton`: 목록 로딩
- `Alert`: 오류 상태

권장 Lucide Icons:

- `Plus`: 새 경험 기록하기
- `Sparkles`: AI 경험 추천 및 활용
- `Filter`: 필터
- `ArrowUpDown`: 정렬
- `CheckCircle2`: 분석 완료
- `Circle`: 미분석
- `RefreshCw`: 재분석 필요
- `ExternalLink`: 관련 링크

### 5.2 1. 활동 경험 작성 / 수정 화면

### 목적

사용자가 활동 경험 원본 데이터를 입력하거나 수정하는 화면입니다. AI 분석 요청은 이 화면에서 실행하지 않습니다.

### 레이아웃

- 중앙 정렬된 단일 폼 레이아웃을 사용합니다.
- 폼 최대 너비는 데스크톱 기준 720~840px 안팎으로 둡니다.
- 저장 / 취소 버튼은 폼 하단에 고정된 액션 영역처럼 배치합니다.

### 필드

- 제목
- 기간
- 역할
- 내용
- 성과
- 관련 링크

### UI 기준

- 필수 입력값은 label과 validation message로 명확히 표시합니다.
- 긴 텍스트 입력은 `Textarea`를 사용합니다.
- 기간은 단순 텍스트 또는 날짜 입력 중 구현 난이도에 맞게 선택하되, 1차 MVP에서는 빠른 입력을 우선합니다.
- 파일 첨부 UI는 포함하지 않습니다.

권장 shadcn/ui:

- `Input`
- `Textarea`
- `Button`
- `Label`
- `Form` 또는 기존 폼 패턴
- `Alert` 또는 inline validation message

권장 Lucide Icons:

- `Save`: 저장
- `X`: 취소
- `Link`: 관련 링크

### 5.3 2. 활동 경험 상세 화면

### 목적

저장된 활동 경험의 원본 내용을 확인하고, 수정하거나 AI 분석을 요청할 수 있게 합니다.

### 정보 우선순위

1. 제목
2. 기간 / 역할
3. 내용
4. 성과
5. 관련 링크
6. AI 분석 상태
7. 수정 / AI 분석 요청 / AI 분석 결과 보기

### UI 기준

- 원본 경험 내용과 AI 분석 상태를 같은 화면에서 확인할 수 있어야 합니다.
- `AI 분석 요청`은 명확한 Primary 또는 강조 버튼으로 제공합니다.
- 분석 결과가 있으면 `AI 분석 결과 보기` 버튼을 제공합니다.
- 수정은 Secondary 액션으로 둡니다.
- 삭제가 구현되는 경우 destructive variant와 확인 다이얼로그를 사용합니다.

권장 shadcn/ui:

- `Button`
- `Badge`
- `Separator`
- `Dialog` 또는 `AlertDialog`
- `Alert`

권장 Lucide Icons:

- `Sparkles`: AI 분석 요청
- `FileText`: AI 분석 결과 보기
- `Pencil`: 수정
- `Trash2`: 삭제
- `ArrowLeft`: 대시보드로 돌아가기

### 5.4 3. AI 경험 분석 결과 화면

### 목적

특정 활동 경험에 연결된 AI 분석 결과를 보여주고, 이후 추천 기능의 기준 데이터로 활용될 수 있음을 명확히 합니다.

### 정보 우선순위

1. 경험 요약
2. 핵심 역량 태그
3. 주요 성과
4. 활용 가능한 키워드
5. 분석 생성일
6. 다시 분석
7. 활동 경험 상세 / 대시보드 / AI 추천으로 이동

### UI 기준

- 분석 결과는 읽기 쉬운 섹션으로 나눕니다.
- 핵심 역량 태그와 키워드는 `Badge`로 표현합니다.
- 주요 성과는 짧은 리스트로 보여줍니다.
- 다시 분석은 Primary가 아니라 보조 액션으로 둡니다.
- AI 추천으로 이동하는 버튼은 Secondary CTA 또는 보조 버튼으로 둡니다.

권장 shadcn/ui:

- `Badge`
- `Button`
- `Separator`
- `Skeleton`
- `Alert`

권장 Lucide Icons:

- `Sparkles`: 분석 결과 / AI 추천
- `Tags`: 태그
- `Trophy`: 주요 성과
- `RefreshCw`: 다시 분석
- `Calendar`: 분석 생성일

### 5.5 4. AI 경험 추천 및 활용 화면

### 목적

사용자가 자기소개서 문항, 포트폴리오 목적, 면접 질문, 기타 활용 목적을 입력하면 저장된 경험 전체와 분석 결과를 참고해 가장 적합한 경험 1개를 추천합니다.

### 레이아웃

- 상단: 활용 목적 선택과 질문 / 문항 입력
- 중단: 추천 요청 버튼
- 하단: 추천 경험 카드 1개와 추천 이유, 활용 방향, 참고 문장

### UI 기준

- 1차 MVP에서는 추천 경험을 1개만 보여줍니다.
- 여러 후보를 비교하는 UI는 만들지 않습니다.
- 복사 기능을 우선합니다.
- 추천 결과 저장은 MVP에 포함하지만, 첫 구현에서는 복사보다 낮은 우선순위로 둡니다.
- 저장된 경험이 없는 경우 먼저 경험을 기록하도록 안내합니다.

권장 shadcn/ui:

- `Select` 또는 `Tabs`: 활용 목적 선택
- `Textarea`: 질문 / 문항 입력
- `Button`: 추천 요청, 복사, 저장
- `Card`: 추천 경험 결과
- `Alert`: 입력 없음 / 경험 없음 / 실패 상태
- `Skeleton`: 추천 요청 중

권장 Lucide Icons:

- `Sparkles`: 추천 요청
- `Copy`: 참고 문장 복사
- `Save`: 추천 결과 저장
- `FileQuestion`: 질문 / 문항
- `ArrowLeft`: 대시보드로 돌아가기

## 6. 컴포넌트 기준

### 6.1 Button

- Primary: 새 경험 기록하기, 저장, AI 분석 요청, 추천 요청
- Secondary: AI 경험 추천 및 활용, 취소, 대시보드로 돌아가기
- Ghost: 필터 열기, 보조 이동
- Destructive: 삭제

버튼 안에는 가능한 경우 Lucide 아이콘을 함께 사용합니다. 단, 모든 버튼에 아이콘을 강제로 넣지 않고, 주요 액션과 의미가 명확한 액션에만 사용합니다.

### 6.2 Badge

분석 상태 배지:

- 미분석: 회색 계열
- 분석 완료: 녹색 또는 청록 계열
- 재분석 필요: 노란색 또는 주황 계열

역량 태그 / 키워드:

- 정보 태그로 표현하되 분석 상태 배지와 색이 섞이지 않게 구분합니다.
- 한 카드에서 태그가 너무 많으면 2~3개까지만 노출하고 나머지는 `+N`으로 축약합니다.

### 6.3 Card / List Item

- 메인 대시보드의 경험 목록은 리스트형 카드로 구현합니다.
- 카드 radius는 8px 이하를 기준으로 합니다.
- 카드 내부에는 제목, 메타 정보, 상태, 태그가 한눈에 들어오게 배치합니다.
- 카드 클릭 영역과 내부 액션이 충돌하지 않게 합니다.

### 6.4 Form

- label은 항상 노출합니다.
- placeholder는 예시를 보조하는 용도로만 사용합니다.
- validation message는 입력 필드 바로 아래에 표시합니다.
- 필수 입력 누락 시 저장하지 않고 안내합니다.

### 6.5 Feedback

- 일시적 성공 안내는 `Toast`를 사용할 수 있습니다.
- 데이터 로딩 실패, AI 요청 실패처럼 사용자가 조치를 해야 하는 상태는 `Alert`로 화면 안에 표시합니다.
- 삭제 확인은 `AlertDialog`를 사용합니다.

## 7. 상태별 UI 기준

### 7.1 저장된 경험이 없는 빈 상태

표시 위치:

- 메인/경험 목록 대시보드의 목록 영역

구성:

- 제목: 아직 기록한 활동 경험이 없습니다
- 설명: 프로젝트, 공모전, 인턴, 대외활동처럼 나중에 다시 활용할 경험을 먼저 기록해보세요.
- Primary CTA: 첫 경험 기록하기

동작:

- `첫 경험 기록하기` 클릭 시 활동 경험 작성 화면으로 이동합니다.

권장 아이콘:

- `NotebookText`
- `Plus`

### 7.2 경험 목록 로딩 상태

표시 위치:

- 메인/경험 목록 대시보드의 카드 목록 영역

구성:

- 리스트 카드 형태의 `Skeleton` 3~5개
- 화면 전체를 막지 않고 목록 영역 중심으로 표시합니다.

### 7.3 localStorage 데이터 불러오기 실패 상태

표시 위치:

- 메인/경험 목록 대시보드 또는 데이터를 읽는 화면

구성:

- 오류 안내 `Alert`
- 다시 시도 버튼
- 대시보드로 돌아가기 버튼

문구 방향:

- 사용자의 잘못으로 보이지 않게 안내합니다.
- 브라우저 저장소 데이터를 불러오지 못했다는 사실과 다시 시도 액션을 명확히 제공합니다.

### 7.4 AI 분석 요청 중 상태

표시 위치:

- 활동 경험 상세 화면 또는 AI 경험 분석 결과 화면 전환 중

구성:

- 버튼 loading 상태
- 분석 결과 영역 `Skeleton`
- 중복 요청 방지를 위한 버튼 disabled 처리

문구:

- AI가 경험을 분석하고 있습니다

### 7.5 AI 분석 실패 상태

표시 위치:

- AI 경험 분석 결과 화면 또는 상세 화면의 분석 영역

구성:

- 실패 안내 `Alert`
- 다시 분석 버튼
- 활동 경험 상세로 돌아가기 버튼

문구 방향:

- 실패 원인을 알 수 없을 때는 간단히 다시 시도 가능성을 안내합니다.
- 입력한 경험 내용은 유지되어야 합니다.

### 7.6 AI 추천 요청 중 상태

표시 위치:

- AI 경험 추천 및 활용 화면의 결과 영역

구성:

- 추천 요청 버튼 loading 상태
- 추천 결과 카드 `Skeleton`
- 중복 요청 방지를 위한 버튼 disabled 처리

문구:

- 저장된 경험을 살펴보고 가장 적합한 경험을 찾고 있습니다

### 7.7 AI 추천 실패 상태

표시 위치:

- AI 경험 추천 및 활용 화면의 결과 영역

구성:

- 실패 안내 `Alert`
- 다시 추천 요청 버튼
- 입력 내용은 유지

### 7.8 분석 완료 상태

표시 위치:

- 메인/경험 목록 대시보드 경험 카드
- 활동 경험 상세 화면

구성:

- `분석 완료` 배지
- 핵심 역량 태그 / 키워드 일부 노출
- AI 분석 결과 보기 버튼

권장 아이콘:

- `CheckCircle2`

### 7.9 재분석 필요 상태

표시 조건:

- 분석 완료 후 활동 내용을 수정해 기존 분석 결과와 원본 내용이 달라질 수 있는 경우

표시 위치:

- 메인/경험 목록 대시보드 경험 카드
- 활동 경험 상세 화면

구성:

- `재분석 필요` 배지
- 다시 분석 버튼 또는 AI 분석 요청 버튼
- 기존 분석 결과가 남아 있더라도 최신 내용 기준이 아닐 수 있음을 짧게 안내

권장 아이콘:

- `RefreshCw`

## 8. 모바일 반응형 기준

### 8.1 브레이크포인트 기준

- 모바일: 0~767px
- 태블릿: 768~1023px
- 데스크톱: 1024px 이상

### 8.2 모바일 대시보드

- 좌측 사이드바는 기본 노출하지 않습니다.
- 상단 앱 바에 CampusLog 이름과 메뉴 버튼을 둡니다.
- 주요 액션은 상단 또는 목록 하단이 아니라 화면 상단에 먼저 노출합니다.
- `새 경험 기록하기`는 Primary CTA로 유지합니다.
- `AI 경험 추천 및 활용`은 Primary CTA 아래 또는 옆에 Secondary CTA로 배치합니다.
- 정렬 / 필터는 한 줄에 배치하되, 좁으면 필터 버튼 하나로 접습니다.
- 경험 카드는 한 열 리스트로 표시합니다.

### 8.3 모바일 폼

- 입력 필드는 한 열로 배치합니다.
- 저장 / 취소 버튼은 하단에 나란히 두거나, 저장 버튼을 전체 너비로 둡니다.
- 긴 textarea는 충분한 높이를 주되 화면을 과도하게 차지하지 않게 합니다.

### 8.4 모바일 AI 결과 / 추천

- 분석 결과 섹션은 세로 스택으로 배치합니다.
- 태그는 줄바꿈 가능한 wrap 구조를 사용합니다.
- 참고 문장 영역은 복사 버튼이 항상 가까이에 있어야 합니다.

## 9. 문구/카피 기준

### 9.1 톤

- 대학생이 부담 없이 기록을 시작할 수 있는 담백한 톤을 사용합니다.
- 기능 설명보다 사용자의 다음 행동을 명확히 안내합니다.
- 과도하게 마케팅적인 표현은 피합니다.

### 9.2 주요 화면 문구

메인/경험 목록 대시보드:

- 제목: 나의 활동 경험
- 설명: 기록한 경험을 다시 보고 AI로 분석하거나 활용해보세요.
- Primary CTA: 새 경험 기록하기
- Secondary CTA: AI 경험 추천 및 활용
- 빈 상태 CTA: 첫 경험 기록하기

활동 경험 작성 / 수정:

- 새 작성 제목: 활동 경험 기록하기
- 수정 제목: 활동 경험 수정하기
- 저장 버튼: 저장
- 취소 버튼: 취소

활동 경험 상세:

- AI 분석 요청: AI 분석 요청
- 결과 보기: AI 분석 결과 보기
- 수정: 수정
- 대시보드로 돌아가기: 대시보드로 돌아가기

AI 추천:

- 추천 요청: 추천 받기
- 복사: 참고 문장 복사
- 저장: 추천 결과 저장

### 9.3 상태 문구

- 미분석
- 분석 완료
- 재분석 필요
- 아직 기록한 활동 경험이 없습니다
- AI가 경험을 분석하고 있습니다
- 추천할 경험을 찾고 있습니다
- 결과를 생성하지 못했습니다. 다시 시도해주세요.

## 10. 1차 MVP에서 제외할 디자인 요소

1차 MVP에서는 아래 UI를 만들지 않습니다.

- 로그인 / 회원가입 화면
- 서비스 소개 전용 랜딩 페이지
- 마이페이지
- 프로필 메뉴
- 사용자 아바타 / 계정 드롭다운
- 팀 / 조직 관리 메뉴
- 업그레이드 / 결제 UI
- 관리자 화면
- 공개 프로필 화면
- 파일 첨부 UI
- 수료증 / 활동사진 업로드 UI
- PDF 포트폴리오 생성 UI
- GitHub / 블로그 / Notion 자동 연동 설정 UI
- Supabase 사용자 / 파일 관리 UI
- 여러 추천 후보 비교 UI
- 복잡한 차트 / 분석 대시보드
- 과한 애니메이션, 3D, 배경 장식 효과

Magic UI, Aceternity UI, Motion 기반 장식 효과는 1차 MVP 필수 범위가 아닙니다. 이후 사용하더라도 버튼 hover, 화면 전환, 로딩 피드백처럼 사용성을 해치지 않는 작은 향상에 한정합니다.

## 11. 이후 구현 체크리스트

메인/경험 목록 대시보드:

- [ ] 첫 화면이 메인/경험 목록 대시보드로 열리는가
- [ ] 저장된 경험 카드 목록이 바로 보이는가
- [ ] 새 경험 기록하기 버튼이 우측 상단 또는 모바일 상단 주요 위치에 있는가
- [ ] AI 경험 추천 및 활용 진입이 대시보드에서 보이는가
- [ ] 정렬 / 필터가 목록 영역과 가까운 곳에 있는가
- [ ] 빈 상태에서 첫 경험 기록하기 CTA가 보이는가
- [ ] 분석 상태 배지가 미분석 / 분석 완료 / 재분석 필요로 구분되는가

활동 경험 작성 / 수정:

- [ ] 작성 / 수정 화면에서 AI 분석 요청 버튼을 제공하지 않는가
- [ ] 저장 후 활동 경험 상세 화면으로 이동하는가
- [ ] 파일 첨부 UI가 없는가
- [ ] 필수 입력값 검증이 화면 안에서 보이는가

활동 경험 상세:

- [ ] 원본 경험 내용이 명확히 보이는가
- [ ] AI 분석 요청이 상세 화면에서 실행되는가
- [ ] 분석 결과가 있으면 결과 보기 버튼이 보이는가
- [ ] 수정 후 재분석 필요 상태를 표시할 수 있는가

AI 경험 분석 결과:

- [ ] 경험 요약, 핵심 역량 태그, 주요 성과, 활용 가능한 키워드, 분석 생성일이 구분되어 보이는가
- [ ] 다시 분석 액션이 보조 액션으로 제공되는가
- [ ] 대시보드와 상세 화면으로 돌아갈 수 있는가

AI 경험 추천 및 활용:

- [ ] 저장된 경험 전체와 분석 결과를 참고하는 별도 흐름으로 보이는가
- [ ] 추천 경험은 1개만 보여주는가
- [ ] 참고 문장 복사 버튼이 추천 결과 근처에 있는가
- [ ] 추천 결과 저장은 복사보다 낮은 우선순위로 배치되는가

범위 확인:

- [ ] 로그인 / 회원가입 UI가 없는가
- [ ] 서비스 소개 랜딩이 없는가
- [ ] 마이페이지 / 프로필 메뉴가 없는가
- [ ] DB / Supabase를 전제로 한 UI가 없는가
- [ ] 파일 업로드 UI가 없는가
